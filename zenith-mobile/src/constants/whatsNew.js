// Shown once to returning users after an update — new users get this via onboarding instead,
// so bumping WHATS_NEW.version alone is enough to re-trigger the modal for the next headline feature.
export const WHATS_NEW = {
  version: "1.1.0",
  icon:    "✦",
  title:   "Prestige, Open to Everyone!",
  body:    "Prestige is no longer PRO-only. Max out any skill and reset it for a permanent REDZONE immunity perk on that skill, plus a stacking XP bonus — no subscription required.",
};

export const WHATS_NEW_KEY = "zenith_whatsnew_seen_version";
